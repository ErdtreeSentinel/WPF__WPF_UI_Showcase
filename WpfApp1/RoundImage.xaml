<UserControl
    x:Class="WpfApp1.RoundImage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp1"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Border
            Width="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:RoundImage}}, Path=ActualWidth}"
            Height="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:RoundImage}}, Path=ActualHeight}"
            ClipToBounds="True"
            CornerRadius="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:RoundImage}}, Path=CornerRadius}">
            <Border.Background>
                <ImageBrush ImageSource="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:RoundImage}}, Path=ImageSource}" Stretch="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:RoundImage}}, Path=Stretch}" />
            </Border.Background>
        </Border>
    </Grid>
</UserControl>
